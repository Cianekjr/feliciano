export interface Viewport {
  width: Number,
  height: Number
}

export interface Link {
  id: Number,
  name: String,
  to: String,
  modifier?: String
}

export interface Category {
  id: Number,
  name: String,
}
